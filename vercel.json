{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build && if [ -n \"$DIRECT_URL\" ]; then prisma migrate deploy; else echo \"DIRECT_URL not set; skipping prisma migrate deploy\"; fi",
  "installCommand": "npm install",
  "regions": ["iad1"]
}
